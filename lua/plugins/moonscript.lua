-- Since moonscript has no lsp or linting, this at least adds some syntax highlighting and stuff
return {
  'leafo/moonscript-vim',
  -- dependencies = { 'nvim-lua/plenary.nvim' },
  init = function() end,
}
